name: NodeJS with Webpack

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x] # Uses a stable Node version

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm' # Speeds up builds

    - name: Install dependencies
      run: npm ci # Cleaner and faster than 'npm install' for CI environments

    - name: Build with Webpack
      run: npm run build # Triggers the build script from your package.json
